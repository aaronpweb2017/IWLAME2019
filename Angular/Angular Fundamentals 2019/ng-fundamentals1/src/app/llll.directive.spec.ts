import { LlllDirective } from './llll.directive';

describe('LlllDirective', () => {
  it('should create an instance', () => {
    const directive = new LlllDirective();
    expect(directive).toBeTruthy();
  });
});
